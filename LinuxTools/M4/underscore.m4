This is a very `SOMETHING' M4 example. A macro is defined for
'SOMETHING', but two substitutions are intended, one for `SOMETHING',
the other for `SOMETHING_ELSE' which is changed to format(`SOMETHING_%s', ELSE) using
a command line options.
