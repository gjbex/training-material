#ifndef FAC_HDR
#define FAC_HDR

/*!
  \brief compute the factorial of the given number
*/
unsigned long fac(unsigned long n);

/*!
  \brief compute the number of combinations of p objects out of n
*/
unsigned long combinations(unsigned long n, unsigned long p);

#endif
