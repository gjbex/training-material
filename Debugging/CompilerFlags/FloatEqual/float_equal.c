#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    if (sqrt(2.0)*sqrt(2.0) == 2.0)
        printf("hooray!\n");
    else
        printf("too bad!\n");
    return EXIT_SUCCESS;
}
