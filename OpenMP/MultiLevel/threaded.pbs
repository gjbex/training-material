#!/usr/bin/env bash
#PBS -A lp_sys
#PBS -l nodes=1:ppn=4
#PBS -l pmem=10gb
#PBS -l walltime=02:00:00
#PBS -j oe

module purge
module load HDF5/1.10.1-intel-2018a

cd $PBS_O_WORKDIR

export LD_LIBRARY_PATH=".:$LD_LIBRARY_PATH"

for file in $(ls test_*.h5)
do
    time ./svd_threaded.exe "${file}" matrix
done
