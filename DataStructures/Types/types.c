#include <float.h>
#include <limits.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    printf("int: %d .. %d\n", INT_MIN, INT_MAX);
    printf("long: %ld .. %ld\n", LONG_MIN, LONG_MAX);
    return EXIT_SUCCESS;
}
